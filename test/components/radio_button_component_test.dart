import "package:test/test.dart";
import 'package:mockito/mockito.dart';
import "dart:html";
import "dart:async";
import '../../lib/nest_ui.dart';

@TestOn("browser")

void main() {

  setUp(() {
    
  });

  test("sets value from DOM and selects the corresponding option", () {
  });

  test("loads all options and their DOM elements from DOM into the #options List", () {
  });

  test("sets value from the selected DOM option element", () {
  });

}
